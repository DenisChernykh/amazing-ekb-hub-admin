import { PlacesList } from '@/widgets/places-list/ui/places-list'

/**
 * Тонкая places page, делегирующая экранную композицию виджету списка мест.
 */
export function PlacesPage() {
  return <PlacesList />
}
