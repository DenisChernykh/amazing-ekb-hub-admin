import { AdminRouteNotFound } from '@/widgets/admin-route-not-found/ui/admin-route-not-found'

/**
 * Тонкая page для неизвестных защищенных admin routes.
 */
export function NotFoundPage() {
  return <AdminRouteNotFound />
}
