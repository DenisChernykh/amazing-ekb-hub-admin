import { AuthLoginScreen } from '@/widgets/auth-login/ui/auth-login-screen'

/**
 * Тонкая login page, делегирующая экранную композицию auth widget.
 */
export function LoginPage() {
  return <AuthLoginScreen />
}
